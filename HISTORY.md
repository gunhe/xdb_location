# History

本文件记录 `xdb_location` 的主要版本变化。没有明确功能变更的版本，会标注为发布流程或版本号维护。

## 0.1.37

* 新增虚拟号段查询模块 `virtual_phone`。
* 新增 `virtual_number_searcher(phone_number)`，用于查询手机号是否命中虚拟运营商号段库。
* 虚拟号查询结果新增 `is_virtual_number` 字段：命中号段库为 `True`，未命中为 `False`。
* 虚拟号查询结果新增 `phone_number` 字段，用于返回原始查询手机号。
* 新增虚拟号查询 README 使用文档。
* 打包配置新增虚拟号段 CSV 数据文件。

## 0.1.36 (2026-01-04)

* 发布版本 `0.1.36`。
* 调整发布脚本和项目版本配置。

## 0.1.35 (2026-01-04)

* 发布版本 `0.1.35`。
* 调整发布脚本和项目版本配置。

## 0.1.34 (2026-01-04)

* 发布版本 `0.1.34`。
* 调整发布脚本和项目版本配置。

## 0.1.20 (2026-01-04)

* 发布版本 `0.1.20`。

## 0.1.19 (2026-01-04)

* 修复版本号提交。
* 更新 History 记录。

## 0.1.10 - 0.1.18 (2026-01-04)

* 调整 GitHub Actions 发布流程。
* 新增 `publish.yml` 发布工作流。
* 更新发布依赖和开发依赖配置。
* 调整 `src/xdb_location/xdb_location.py` 中的部分兼容逻辑。

## 0.1.9 (2026-01-04)

* 添加 CI/CD 任务。

## 0.1.8 (2026-01-04)

* 降低 Python 最低支持版本到 3.8。

## 0.1.2 - 0.1.7 (2025-10-22)

* 更新 README 使用文档。
* 完善项目元数据和依赖配置。
* 优化 `searchWithContent` 相关加载逻辑。

## 0.1.1 (2025-10-21)

* 初始化离线 IP 地址定位核心实现。
* 新增 xdb 数据读取、索引、segment、maker 等模块。
* 新增 `searchWithContent` 查询能力。
* 新增基础测试用例。

## 0.1.0 (2025-10-10)

* First release on PyPI.
* 初始化项目结构、基础依赖和发布配置。
